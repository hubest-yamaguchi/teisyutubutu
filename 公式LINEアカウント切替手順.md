# テスト用公式アカウント → 本番専用公式アカウントへの切替手順

最終更新：2026-08-28

## 0. 前提（なぜこの手順が必要か）

現在のテスト設定は以下の通り。

| 種類 | チャネル名 | 備考 |
| --- | --- | --- |
| プロバイダー | 入社書類提出 | |
| LINEログイン（LIFF） | test | LIFFアプリ「submit test」（LIFF ID: `2011207061-xN76TCcJ`）を使用中 |
| Messaging API | U-Select佐賀車両問合せ専用窓口 | 本来は車両問合せ用の別事業の公式アカウント。動作確認のため一時的に間借りしている |

**LIFF（ログイン）とMessaging API（通知送信）は、必ず同じプロバイダーの中にある必要がある。** 違うプロバイダーだと、LIFFでログインしたユーザーのLINE IDと、Messaging API側で送信先として指定するLINE IDが一致せず、通知が届かない。

本番運用では、「車両問合せ専用窓口」から入社書類の通知が届くのは紛らわしいため、入社書類提出専用の公式アカウントに切り替える。

## 1. 新しいMessaging APIチャネルを作成する

1. LINE Developersコンソール（`https://developers.line.biz/console/`）を開く
2. プロバイダー一覧から **「入社書類提出」**（testチャネル・LIFFアプリが入っているのと同じプロバイダー）を開く
   - ⚠️ 別のプロバイダーの中に作ってしまうと、また同じ「プロバイダーが違う」問題が再発するので注意
3. 「新規チャネル作成」→「Messaging API」を選択
4. チャネル名・説明・業種などを入力（例：チャネル名「〇〇株式会社 入社書類提出」）
5. 作成完了後、チャネル基本設定の画面で以下を控える
   - Channel ID
   - Channel secret

## 2. LINEログインチャネルに公式アカウントをリンクする（友だち追加を促すため）

このままだと、内定者がLIFFでログインしても新しい公式アカウントを友だち追加しないため、通知が届かない。LINEログイン側の設定で紐付ける。

1. 同じプロバイダー内の **「test」チャネル**（LINEログイン）を開く
2. 「チャネル基本設定」タブを開く
3. 「**リンクされたLINE公式アカウント**」という項目を探し、手順1で作った新しいMessaging APIチャネルを選択して保存
4. 「**友だち追加時のトーク画面遷移**」などのオプションがあれば「オン」にしておく（ログイン後、自動で友だち追加を促す画面が出るようになる）

※チャネル名を「test」のままにしておくと分かりにくいので、このタイミングで「入社書類提出LIFF」などに変更してもよい（チャネル基本設定の一番上、チャネル名の編集）。

## 3. チャネルアクセストークンを発行する

1. 手順1で作った新しいMessaging APIチャネルを開く
2. 「Messaging API」タブを開く
3. 「チャネルアクセストークン（長期）」の発行ボタンを押す
4. 表示されたトークンをコピーする（この画面を閉じると再表示できない場合があるので、必ずこの場でコピーする）

## 4. 管理画面の設定を更新する

1. `https://teisyutubutu.hubest.workers.dev/admin/` を開く（Cloudflare Accessでログイン）
2. 「設定」→「LINE公式アカウント設定」を開く
3. 以下を新しい値に書き換えて保存する
   - チャネルアクセストークン：手順3で発行したもの
   - LIFFチャネルID：`2011207061-xN76TCcJ`（LIFFアプリ自体は変更していないので、この値は変わらない。手順2で公式アカウントを紐付けたことで、この同じLIFF経由のログインが新しい公式アカウントとの友だち追加も兼ねるようになる）

## 5. 動作確認

1. まだ新しい公式アカウントを友だち追加していない状態のLINEアカウント（できればテスト用の別アカウント）で、LIFF URL（`https://liff.line.me/2011207061-xN76TCcJ`）を開く
2. ログイン後、新しい公式アカウントの「友だち追加」画面が表示されることを確認する
3. 追加後、本人確認〜書類提出まで進める
4. 管理画面から差し戻し操作をして、LINEに通知が届くか確認する
5. 管理画面の「通知ログ」（NotificationQueue相当）で送信結果を確認する

## 6. テスト用チャネルの後片付け（任意）

動作確認が終わったら、「U-Select佐賀車両問合せ専用窓口」への参照が残っていないか確認する。

- 管理画面の「設定」に保存したチャネルアクセストークンは、手順4で新しいものに上書き済みなので対応不要
- 「U-Select佐賀車両問合せ専用窓口」チャネル自体は本来の車両問合せ業務で使われている可能性があるため、**削除しない**こと
